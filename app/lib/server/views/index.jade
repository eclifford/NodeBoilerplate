h1= title
p Welcome to #{title}

- if(everyauth.loggedIn)
	//p User.name #{user.name}
	//- p Hello #{everyauth.twitter.user.name}
	//- img(src="#{everyauth.twitter.user.profile_image_url}")
	//-  	img(src="#{everyauth.instagram.user.profile_picture}")
	a(href='/logout') Logout
- else
	a(href="/auth/instagram") Log in with instagram

div#ui
	input(id='txtSearch')
	button(id='btnSearch')
	button(id='btnFlip')
	button(id='btnFlipOn')
	button(id='btnFlipOff')

#instagramapp

#detailpanel

//- div#wrapper
//- 	!= partial('partials/image', images)

script(type='text/x-jquery-tmpl', id='image-template')
	.front
		img(src='${images.thumbnail.url}')
	.back(style='background-position: -${xpos}px -${ypos}px')



